# Local secret is generated in .dev.vars by scripts/setup-accounts.mjs.
# Hosted deployments require these two runtime values in the hosting environment.
VESSEL_AUTH_URL=http://localhost:3000
VESSEL_AUTH_SECRET=
